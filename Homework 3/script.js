var name = prompt("Как тебя зовут?)");

if(name.length <= 6) {
	alert("Привет, " + name);
}
if else(name.length == null) {
	alert("Вы не указали своё имя");
}
else {
	alert("Добрый день, " + name);
}
